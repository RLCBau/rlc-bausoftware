import express from "express";

const app = express();
const PORT = 4000;

app.get("/", (req, res) => {
  res.send("✅ Backend läuft – RLC Bausoftware API ist online!");
});

app.listen(PORT, () => {
  console.log(`Server läuft auf http://localhost:${PORT}`);
});
