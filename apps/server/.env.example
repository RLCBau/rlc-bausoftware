# Datenbank
DATABASE_PROVIDER=postgresql  # postgresql | sqlite | mysql
DATABASE_URL=postgresql://user:password@localhost:5432/rlc?schema=public

# Für SQLite (Alternative)
# DATABASE_PROVIDER=sqlite
# DATABASE_URL=file:./dev.db
PORT=4000

# SMTP (per invio offerta)
SMTP_HOST=smtp.yourhost.tld
SMTP_PORT=587
SMTP_USER=postmaster@yourhost.tld
SMTP_PASS=yourpassword
MAIL_FROM="RLC Bausoftware <noreply@rlc-bau.de>"

# OpenAI (per KI reale)
OPENAI_API_KEY=sk-xxx
OPENAI_MODEL=gpt-4.1-mini
